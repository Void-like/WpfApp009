<Window x:Class="WpfApp009.AdminView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp009"
        mc:Ignorable="d"
        Title="AdminView" Height="450" Width="800">
    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="АДМИНИСТРАТОРСКАЯ ПАНЕЛЬ" 
                       FontSize="24" 
                       FontWeight="Bold"
                       Margin="0,0,0,20"/>

            <Button Content="Управление пользователями" 
                    Width="250" 
                    Height="40"
                    Margin="0,10"
                    Style="{StaticResource ModernButton}"/>

            <Button Content="Настройки системы" 
                    Width="250" 
                    Height="40"
                    Margin="0,10"
                    Style="{StaticResource ModernButton}"/>

            <Button Content="Статистика" 
                    Width="250" 
                    Height="40"
                    Margin="0,10"
                    Style="{StaticResource ModernButton}"/>
        </StackPanel>
    </Grid>
</Window>

